server:
  port: 8085
#redis数据源地址
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/test?useSSL=false
    password: root
    username: root

  thymeleaf:
    cache: false
    mode: HTML

    prefix: classpath:/templates/ # Prefix that gets prepended to view names when building a URL.
    suffix: .html
 # redis:
 #   host: 127.0.0.1
 #   jedis:
  ##    pool:
   #     max-active: -1
   #     max-idle: 500
   #     min-idle: 50
   #     max-wait: -1s
    driver-class-name: com.mysql.jdbc.Driver
  #mysql数据源地址

#配置缓存方式为redis
 # cache:
  #  type: redis
mybatis-plus:
  mapper-locations: classpath:com/jiang/redis/mapper/xml/*.xml


